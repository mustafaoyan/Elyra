name: Build Linux release

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  deb:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Debian/Pardus package
        working-directory: SIPER-stage15.3
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev python3-venv
          python3 -m venv .release-venv
          . .release-venv/bin/activate
          python -m pip install --upgrade pip build
          bash scripts/build_pardus_deb.sh dist/debian
      - name: Generate release checksums
        working-directory: SIPER-stage15.3
        run: sha256sum dist/debian/*.deb > dist/debian/SHA256SUMS
      - name: Upload GitHub release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            SIPER-stage15.3/dist/debian/*.deb
            SIPER-stage15.3/dist/debian/SHA256SUMS
