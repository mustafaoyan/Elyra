# Packaging evidence

Store screenshots and text records for:

- final source/submission archive creation;
- Python wheel creation;
- Debian package build;
- `dpkg-deb --info` output;
- package installation and service status;
- generated SHA-256 checksums.
