# Environment evidence

`finalize_submission.py` writes `final_environment_pardus.json` here. The record
contains the project version, Pardus release, kernel, architecture, Python
version, effective UID, timestamp and Git commit when available.
